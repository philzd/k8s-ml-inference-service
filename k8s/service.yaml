apiVersion: v1
kind: Service
metadata:
  name: k8s-ml-inference
  namespace: k8s-ml-inference
spec:
  selector:
    app: k8s-ml-inference
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: ClusterIP
